P5 — разбор исторических докладов

Набор из шести имён, отмеченный предыдущим аудитом как неразобранный:
tick135-report.md, tick197-report.md, tick198-report.md, tick200-report.md,
tick201-report.md, tick218-report.md.

Текущий progress_guard разбирает tick197, tick198 и tick200: в них есть
машинные строки гейта, регрессии и BBLM. Остались три честно неразобранных
доклада:

- tick135-report.md: гейт завершился до машинной строки с числовым итогом;
  дальнейшие фазы не запускались, поэтому ось матрицы, регрессия и BBLM
  отсутствуют. Статус not-evaluated.
- tick201-report.md: в тексте есть регрессия и старый счётчик BBLM, но нет
  числового итога гейта и итога ОС-матрицы. Статус not-evaluated.
- tick218-report.md: есть регрессия, но отсутствуют числовой итог гейта и
  машинная строка BBLM/ОС-матрицы. Статус not-evaluated.

Заполнение пропущенных чисел по соседним тикам запрещено: молчание проверки
не является покрытием. Причина остатка — формат старых докладов, а не новый
провал инструмента.
