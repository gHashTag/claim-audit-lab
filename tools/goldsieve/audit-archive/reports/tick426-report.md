# Тик 426 — 2026-09-08 — непрерывный аудит «золотое сито»

## (1) что исправлено в инструменте

Исправлен `regression_cache_guard.py`, последний сторож без печатаемых статусов. Режимы `--audit` и `--scan` теперь читают настоящие снимки `baseline/regression-file-cache.json` и `baseline/regression-fingerprints.json`, проверяют форму записей, наличие 175 файлов и совпадение их `size`, `mtime_ns`, `ctime_ns` и `inode` с текущими входами. При полном совпадении печатается `статус входа: verified-in-scope`; отсутствие, повреждение или устаревание входа получают `not-evaluated` либо `unsupported` и не становятся покрытием. Самопроверка дала 13 пройдено и 0 провалено. Проверка эффективности сторожа завершилась кодом 0; полный гейт содержательности подтвердил 0 пустых сторожей при замороженных 13.

**чем этот тик отличается от предыдущего:** предыдущий тик перевёл `provenance_guard.py` из безстатусного режима в класс «устанавливает», предъявив два реальных входа корпуса; этот тик перевёл последний безстатусный `regression_cache_guard.py` в тот же класс, предъявив два настоящих снимка кэша и актуальность 175 файлов. Новых сторожей, внешних целей и внешних констант не добавлялось; `os_matrix_audit.py` не изменялся. Ресурсный сторож завершился кодом 0. Обязательный гейт до правки завершён кодом 0: 150 шагов, 2 штатных пропуска без `numpy/pyyaml` в CPython 3.12, 0 провалов, 162 с. Финальный гейт завершён кодом 0: 150/2/0, 145 с. Инкрементальный регресс: 7 выбрано, 163 пропущено, 7 совпало, 0 изменений ситом, 0 изменений корпусом, 0 несопоставленных; кэш — 175 попаданий и 0 пересчётов. Проверено на указанных версиях CPython; платформенная универсальность не заявляется. Сохранены `STANDARD_RANGES=20412`, `ACTUAL_RANGES=123201`, каталог = 83 формата, аппаратное объединение около 49–55/83, кремний = отправлен на изготовление.

## (2) что установлено о zeta/GUE

Нового научного результата о zeta/GUE этот тик не устанавливает. В корпусе сохраняется наблюдение около 100000 нулей со стандартным отклонением зазоров около 0,4009; число 0,4220 остаётся приближением Вигнера—сюрмиса, а предъявленный точный эталон GUE равен 0,4242576222440628. Перевод сторожа кэша в `verified-in-scope` устанавливает только область и актуальность машинных входов регресса; он не пересчитывает нули, не доказывает независимость путей и не подтверждает универсальность GUE.

## (3) что осталось недоказанным

Однозначность рецепта zeta имеет статус `not-evaluated`: предъявлены 14 вариантов, 11 воспроизводят 0,4009. Общий `M_eff` остаётся `not-evaluated` для 12 архивов с несколькими записями С20. Предпосылка независимости остаётся `not-evaluated` для 132 из 133 кейсов и `unsupported` для одного. В протоколе BBLM предъявлены 7 из 8 элементов, но аналитический источник коэффициентов отсутствует: `analytic_source_absent`. Смысл `χ²/dof`, шаровая арифметика Arb, криптографическая подпись журнала и область её действия остаются `not-evaluated`.

ОС-матрица остаётся `platform-unverified`, 0/6; это не вывод о переносимости. Долг повторов внешних целей остаётся 7 группами и не вырос. Молчание проверки не считается покрытием; допустимые статусы — `verified-in-scope`, `not-evaluated`, `unsupported`, `platform-unverified`.

## (4) какие артефакты и тесты это подтверждают

Исходник и машинный отчёт: `/home/user/workspace/goldsieve/regression_cache_guard.py`, `/home/user/workspace/goldsieve/regression_cache_guard.json`. Самопроверка записана в `/home/user/workspace/cron_tracking/20fee222/tick426-cache-selftest.txt`, настоящий вход и статус — в `/home/user/workspace/cron_tracking/20fee222/tick426-cache-audit.txt`, проверка эффективности с кодом 0 — в `/home/user/workspace/cron_tracking/20fee222/tick426-efficacy-check.txt`, полный гейт содержательности — в `/home/user/workspace/cron_tracking/20fee222/tick426-efficacy-gate.txt`.

Обязательный гейт и его код подтверждены `/home/user/workspace/cron_tracking/20fee222/tick426-gate.log` и `/home/user/workspace/cron_tracking/20fee222/tick426-gate.rc`; финальный гейт — `/home/user/workspace/cron_tracking/20fee222/tick426-post-gate.log` и `/home/user/workspace/cron_tracking/20fee222/tick426-post-gate.rc`. Регресс подтверждён `/home/user/workspace/cron_tracking/20fee222/tick426-regression.log` и `.rc`; обновление манифеста, снимок baseline и снимок Trinity — `tick426-coverage-update.txt`, `tick426-baseline-snapshot.txt`, `tick426-tri-snapshot.txt`. Машинная суть `/home/user/workspace/cron_tracking/20fee222/tick426-progress-substance.json` прошла `progress_guard.py --check` и `--record` как `СОДЕРЖАТЕЛЬНЫЙ`, подпись `f3e589b8efae8683`; синхронность после коммита `524a600` подтверждена `tick426-repo-sync.txt` кодом 0 и 239 совпадающими файлами. Коммит отправлен только в ветвь `tools/goldsieve-v3-2026-08-13`. Технический счётчик зарегистрировал два вызова `gate_closed` без `--note`; это нарушение наблюдаемости самого запуска, а не результат проверки корпуса, и оно сохранено в журнале счётчиков.
