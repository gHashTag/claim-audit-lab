# Тик 48 — отказ бюджета для повторного элементарного p-value

## Контракт тика

- **Цель:** после закрытия пунктов 1–5 приказа выбрать одну следующую цель по порядку приоритета и не добавлять дешёвое табличное подтверждение для повторяющегося элементарного p-value.
- **Входы:** рабочая копия `/home/user/workspace/goldsieve`; реестр `claims.yaml`; корпус `/home/user/workspace/corpus/trinity`; чекпоинт чанковой разведки; прежний паспорт цели `P(X <= 69) при Poisson(lambda=286)`.
- **Ожидаемый артефакт:** точный вывод гейта и регресса, продолжение cover от чекпоинта, кодовое решение бюджета и предфильтра, применение SLA матрицы ОС, запись в ведомость и счётчики.
- **Критерии приёмки:** гейт открыт; регресс завершён в фоне; каждое отклонение имеет код в `baseline/reasons.yaml`; cover не выносит вердикт; предфильтр выполнен до дорогого сита; отказ бюджета не записан как научный вердикт; статусы не расширены.
- **Бюджет:** одна цель; повтор класса элементарного p-value допускается только при новом классе рецепта, новом источнике, новой категории ошибки или новом риске публичного утверждения.

## Разведка → план → исполнение → верификация → доклад

1. **Разведка.** Состояние предыдущего запуска и ведомость прочитаны. Пункты 1–5 приказа закрыты: паспорт развёртки дзеты, проверка сопоставимости метрик перплексии, кодовый SLA ОС, чанковая разведка и бюджет новизны. Счётчик `tick_started` увеличен до 48.
2. **Гейт.** `timeout 1800 ./ci_gate.sh` завершён с кодом 0. Точная итоговая строка: `ГЕЙТ ОТКРЫТ: слияние разрешено`. CPython 3.14.3 и 3.14.4 прошли полный контур; CPython 3.12.13 получил объявленные пропуски каскада и калибровки из-за отсутствия `numpy/pyyaml`.
3. **Регресс.** Обязательный фоновый запуск `setsid nohup timeout 3000 python3 -m goldsieve regress --registry claims.yaml` завершён. Точный итог: `перепрогон реестра: 127 совпало, 0 изменилось ситом, 3 изменилось из-за корпуса, 0 не сопоставлено`. Все три отклонения покрыты существующими кодами: `corpus-fixed` для раздела Established Constants и `corpus-data-changed` для двух формул отношений масс. В `baseline/reasons.yaml` новых отклонений нет.
4. **Чанковая разведка.** Выполнено продолжение от `/home/user/workspace/cron_tracking/20fee222/cover-checkpoint.json` размером чанка 64 и коротким тайм-аутом 20 секунд. Получено `1804` файлов, `9263` константы, `0` тайм-аутов, `complete=true`; последний успешно обработанный файл — `tools/bin/repo-root/README.md`. Это триаж, а не аудит и не научный вердикт. Отпечаток чекпоинта: `db04c41687413681`.
5. **План и кодовый предфильтр.** Выбран прежний кандидат `P(X <= 69) при Poisson(lambda=286) = 1.46894459373474e-53`, потому что после закрытых пунктов и инженерных OPEN-задач он остаётся ближайшим доступным повтором. Код бюджета зафиксировал отсутствие всех четырёх оснований новизны: новый класс рецепта — нет; новый источник — нет; новая категория ошибки — нет; новый риск для публичного утверждения — нет. Предфильтр на однородной паре `STANDARD_RANGES`, `M=20412`, вернул `SKIP-VOID`: локальная относительная погрешность `inf`, ожидаемых случайных попаданий `inf`, порог Шидака `4,71` сигма. JSON решения: `tick48_prefilter.json`, SHA16 `6f02017813c6619c`; SHA16 входов сохранены как `2ab076ef8674c777` и `e8f915354f99df26`.
6. **Решение о запуске.** Бюджет информационной новизны отказал. Дорогое сито не запускалось, новый кейс и claim не создавались, `claims.yaml`, код инструмента, манифест покрытия, baseline и снимок TRI не менялись. Это решение о запуске, а не вердикт по утверждению.
7. **ОС-матрица и SLA.** Доступного runner нет, `total_attempts_left=0`, повтор `pc bash` запрещён. `python3 -m goldsieve.platform_sla pending/os-matrix.yaml` принял `scope_status: platform-unverified` после двух последовательных `not-evaluated`; Windows остаётся `not-evaluated`. Счётчик `tick_aborted_other` увеличен до 22, `tick_completed` после этой записи увеличен до 48.

## Что ДОКАЗАНО

- На указанных версиях CPython гейт открыт; полный регресс дал `127/0/3/0`, а три изменения корпуса имеют коды `corpus-fixed` и `corpus-data-changed`.
- Чанковый cover продолжен от чекпоинта и завершён без тайм-аутов: `1804/1804`, `9263`, `complete=true`.
- Кодовый бюджет новизны и предфильтр выполнены до дорогого сита; повторный элементарный p-value получил решение `ОТКАЗ` и `SKIP-VOID`.
- Новый научный вердикт не сформирован; `claims.yaml` не изменён.
- Кодовый SLA применён: macOS имеет `platform-unverified`, Windows — `not-evaluated`; расширенный `verified-in-scope` не выдавался.
- Формулировка статуса инструмента сохранена: «инструмент стабилен на Linux/CPython, выявляет реальные несоответствия в корпусе и поддерживает воспроизводимый регресс; межплатформенная верификация пока неполна».

## Что НЕ доказано

- Не доказана истинность или ложность p-value в новом запуске: дорогое сито намеренно не выполнялось.
- Не доказано, что повторный рецепт элементарного p-value переносится на другой источник, реализацию, выборку или рантайм.
- Не доказана полная ОС-матрица: macOS имеет `platform-unverified`, Windows — `not-evaluated`.
- Не доказан универсальный порог Шидака: `4,71` сигма относится только к решению предфильтра и не является научным вердиктом.

Уведомление не отправляется: нового `ОПРОВЕРГНУТО` и находки о дефекте самого инструмента нет.
